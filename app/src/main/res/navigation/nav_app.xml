<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_app"
    app:startDestination="@id/listFragment">

    <fragment
        android:id="@+id/listFragment"
        android:name="com.example.movieapiretrofit.ListFragment"
        android:label="fragment_list"
        tools:layout="@layout/fragment_list" >
        <action
            android:id="@+id/action_listFragment_to_filterFragment2"
            app:destination="@id/filterFragment2" />
    </fragment>
    <dialog
        android:id="@+id/filterFragment2"
        android:name="com.example.movieapiretrofit.FilterFragment2"
        android:label="fragment_filter2"
        tools:layout="@layout/fragment_filter" />
</navigation>